# Notebooks

Optional exploratory notebooks. Keep the *reproducible* pipeline in `R/` and
`python/`; use this folder for EDA, figure drafts, and the thesis results
chapter. Suggested:

- `eda.ipynb` — distribution of species prevalence/abundance, class balance,
  metformin coverage per cohort.
- `results.ipynb` — pull `artifacts/metrics.json` + `artifacts/shap_ranked.csv`
  into the figures and tables for the thesis.
