# Figures

Generated by `python/04_interpret_shap.py` (git-ignored PNGs):

- `shap_beeswarm.png` — global SHAP summary with direction (which taxa raise/lower predicted T2DM risk).
- `shap_bar.png` — mean(|SHAP|) ranked importance.
- `shap_waterfall_patient*.png` — per-patient, clinically readable explanations.
- `shap_dependence_*.png` — nonlinearity / interaction (e.g., *Akkermansia* × metformin).

Regenerate rather than commit; only curated final figures should be versioned.
